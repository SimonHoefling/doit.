<!DOCTYPE html>
<html>
  <head>
    <title>Doit</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%# The next 2 lines are for the mapbox map %>
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.11.0/mapbox-gl.js"></script>
    <%# The next 4 lines are for the PWA setup %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
  </head>

  <body>
    <%# This condition excludes the flashes on the chatroom show page %>
    <%# unless controller_name == 'chatrooms' && (action_name == 'show' || action_name == 'index') %>
      <%#= render "shared/flashes" %>
    <%# end %>
    <%= render "shared/navbar_desktop" %>
    <%= render "shared/pwa_instruction" %>

    <%= yield %>

    <%# This conditon excludes the mobile navbar on the chatroom show page %>
    <% unless controller_name == 'chatrooms' && action_name == 'show' %>
      <%= render "shared/navbar_mobile" %>
    <% end %>
  </body>
</html>
